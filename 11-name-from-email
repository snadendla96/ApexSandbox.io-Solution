public String nameFromEmail(String email) {
    //code here
    String firstName=email.substringBefore('.');
    String lastName=substringBetween('.');
    System.debug(lastName);
    return toLowerCase(firstName+''+lastName);
}