public String aOrAn(String word) {
    char firstChar = word.toLowerCase().charAt(0); // Case-insensitive check

    if (firstChar == 'a' || firstChar == 'e' || firstChar == 'i' || firstChar == 'o' || firstChar == 'u') {
        return 'an ' + word;
    } else {
        return 'a ' + word;
    }
}