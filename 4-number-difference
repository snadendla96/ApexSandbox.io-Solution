public Integer diff(Integer a, Integer b) {
    //code here
    Integer diff;
    if(a>b){
        System.debug('a is bigger'+a);
        diff= a - b;
    }else if(b>a){
        System.debug('b is bigger'+b);
        diff= b - a;
    }
    return diff;
}